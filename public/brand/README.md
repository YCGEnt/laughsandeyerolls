# Brand Assets

Place the supplied SVG wordmark assets in this directory using these exact filenames:

- `Espresso-wordmark-primary.svg` — hero sections
- `Espresso-wordmark-compact.svg` — navigation, footer, mobile headers, and constrained spaces

Do not recreate the wordmark with typography. The application references these SVG files directly.
